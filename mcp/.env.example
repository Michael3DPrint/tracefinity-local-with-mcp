# Reference only — not auto-loaded. Set these in .mcp.json's "env" block or export
# them before running verify.py. All have working defaults.

# Where the MCP reaches Tracefinity.
TRACEFINITY_BASE_URL=http://localhost:3000

# Overrides for the package-relative defaults (mcp/secrets, mcp/downloads).
# TRACEFINITY_SECRET_DIR=/abs/path/to/secrets
# TRACEFINITY_DOWNLOAD_DIR=/abs/path/to/downloads
